import { FormatCardBankPipe } from './format-card-bank.pipe';

describe('FormatCardBankPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardBankPipe();
    expect(pipe).toBeTruthy();
  });
});
